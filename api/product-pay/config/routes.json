{
  "routes": [
    {
      "method": "GET",
      "path": "/product-pays",
      "handler": "product-pay.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/product-pays/count",
      "handler": "product-pay.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/product-pays/:id",
      "handler": "product-pay.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/product-pays",
      "handler": "product-pay.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/product-pays/:id",
      "handler": "product-pay.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/product-pays/:id",
      "handler": "product-pay.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
